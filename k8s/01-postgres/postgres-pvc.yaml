apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: distributed-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Pour Minikube, storageClassName peut être omis ou utiliser 'standard'
  storageClassName: standard